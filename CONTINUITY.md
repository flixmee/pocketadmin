Goal (incl. success criteria):

- Add Telegram credentials plus Telegram automation support.
- Success: Settings can store/test Telegram credentials; automations can send Telegram messages; automation editor offers an "On Telegram message" trigger; inbound Telegram webhook/message payloads can start active Telegram-message automations; tests cover execution.

Constraints/Assumptions:

- Follow `AGENTS.md` and UI guidance.
- Existing unrelated changes may be present; do not revert them.
- When updating admin UI, consult `UI_DOCS.md`.
- Never introduce native HTML `<select>` elements in the admin UI; use `app.components.select`.
- Keep Go/backend and UI changes isolated unless persistence requires both.
- Credential schema/persistence requirements are UNCONFIRMED; infer from existing settings patterns before editing.

Key decisions:

- Add `credentials` to the core settings model so the data persists with existing Settings APIs.
- Credentials include Telegram (`enabled`, `baseURL`, `accessToken`) and Google Sheets (`enabled`, `oauthRedirectURL`, `clientID`, `clientSecret`).
- Redact `accessToken` and `clientSecret` from Settings API JSON responses.
- Add a dedicated admin UI page at `#/settings/credentials` under Settings > System.
- Add dedicated test endpoints that accept unsaved credential payloads and return no content on successful verification.
- Add a Telegram automation step using configured Telegram credentials.
- Add a Telegram-message automation trigger using configured Telegram credentials.
- Add a user-facing action to register the Telegram webhook and display Telegram API results.

State:
  - Done:
    - Read `CONTINUITY.md`.
    - Read relevant `UI_DOCS.md` admin UI conventions.
    - Replaced prior flow designer ledger with the new Credentials goal.
    - Added backend `CredentialsConfig` with validation and redaction.
    - Added model/API test expectations for persistence and secret redaction.
    - Added `ui/src/settings/credentials/pageCredentialsSettings.js`.
    - Added Settings sidebar entry and route for `#/settings/credentials`.
    - Ran `gofmt` on touched Go files.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/settings/credentials/pageCredentialsSettings.js src/store.js src/router.js`; passed.
    - Ran `node --check src/settings/credentials/pageCredentialsSettings.js`; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`; passed with existing Vite large chunk warning.
    - Ran `go test ./core ./apis -run 'TestSettings'`; passed when rerun with normal permissions for local test listeners.
    - Ran `git diff --check`; passed.
    - Ran `go test ./...`; failed in existing-looking automation/OTP scheduler tests outside the credentials change (`TestRecordAuthWithOTPManualRateLimiterCheck`, automation delay scheduler panic).
    - Removed untracked `core/pb_base_app_test_data_dir/` generated by the failed full test run.
    - Received follow-up request to test credentials before save.
    - Added credential test routes: `POST /api/settings/test/telegram` and `POST /api/settings/test/google-sheets`.
    - Added Telegram credential test flow: `getMe` plus `sendChatAction` using the submitted/current chat id.
    - Added Google Sheets credential test flow: service-account JWT token exchange plus spreadsheet metadata request.
    - Credential test handlers merge omitted redacted secret fields from stored settings.
    - Added per-provider Test buttons on `ui/src/settings/credentials/pageCredentialsSettings.js`.
    - Added focused credential helper/API tests in `apis/settings_credentials_test.go` and `apis/settings_test.go`.
    - Ran `go test ./apis -run 'TestSettingsTestCredentials|TestTestTelegramCredentials|TestTestGoogleSheetsCredentials'`; passed.
    - Ran `go test ./core ./apis -run 'TestSettings'`; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`; passed.
    - Reverted generated `ui/dist/index.html` asset hash churn from the verification build.
    - Received correction: Telegram fields should be Base URL + Access token; Google Sheets account fields should be OAuth Redirect URL + Client ID + Client Secret.
    - Updated credential settings schema to Telegram (`baseURL`, `accessToken`) and Google Sheets (`oauthRedirectURL`, `clientID`, `clientSecret`).
    - Updated redaction from old `botToken`/`serviceAccountJSON` to `accessToken`/`clientSecret`.
    - Updated Telegram test-before-save to call `getMe` with the configured Base URL and Access token.
    - Updated Google Sheets test-before-save to validate the OAuth client fields through the token endpoint using a synthetic auth code; `invalid_grant` is treated as successful credential shape/client validation.
    - Updated Credentials UI fields to exactly match requested labels.
    - Updated model/API/helper tests and JSON snapshots for the corrected fields.
    - Ran `go test ./apis -run 'TestSettingsTestCredentials|TestTestTelegramCredentials|TestTestGoogleSheetsCredentials'`; passed.
    - Ran `go test ./core ./apis -run 'TestSettings'`; passed.
    - Ran `node --check src/settings/credentials/pageCredentialsSettings.js`; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`; passed with existing Vite large chunk warning.
    - Ran `git diff --check`; passed.
    - Reverted generated `ui/dist/index.html` asset hash churn from the verification build.
    - Received follow-up request to add an automation step that sends a Telegram message.
    - Added backend Telegram automation step type, schema, validation, dry-run preview, and execution dispatch.
    - Added Telegram automation sender using Settings > Credentials Telegram `baseURL` and `accessToken`.
    - Added admin automation editor support for `telegram.send` with Chat ID, Message, Parse mode, and Disable web page preview fields.
    - Added focused Telegram execution test using a local fake Telegram API.
    - Ran `gofmt` on touched automation Go files; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/settings/automations/stepEditor.js src/settings/automations/telegramStepForm.js`; passed.
    - Ran `node --check src/settings/automations/stepEditor.js` and `node --check src/settings/automations/telegramStepForm.js`; passed.
    - Ran `go test ./core -run 'TestAutomationTelegramStepSendsMessage|TestAutomationSchemas|TestAutomationValidate'`; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`; passed with existing Vite large chunk warning.
    - Reverted generated `ui/dist/index.html` asset hash churn from the verification build.
    - Ran `git diff --check`; passed.
    - Received follow-up request to add an automation trigger for Telegram messages.
    - Added `telegram.message` automation trigger type, schema, validation, and template root support.
    - Added public Telegram webhook route `POST /api/automation-telegram/{accessToken}` that verifies Settings > Credentials Telegram access token and queues active Telegram-message automations.
    - Added Telegram trigger payload under `telegram`, including `update`, `updateId`, `messageType`, `message`, `chat`, `from`, and normalized `text`.
    - Added admin UI trigger option "On Telegram message" and display/copy endpoint pattern `POST /api/automation-telegram/<access-token>`.
    - Added Telegram mapping autocomplete tokens such as `{{telegram.text}}`, `{{telegram.chat.id}}`, and `{{telegram.from.username}}`.
    - Added focused core test for queued Telegram message automation runs and API test for Telegram webhook route/token validation.
    - Ran `gofmt` on touched automation/API Go files; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/base/automationInput.js src/settings/automations/pageAutomationUpsert.js src/settings/automations/automationUpsertModal.js src/settings/automations/automationsList.js src/settings/automations/automationRunsList.js src/settings/automations/automationRunPreviewModal.js`; passed.
    - Ran `node --check` for changed automation UI modules; passed.
    - Ran `go test ./apis -run 'TestAutomationTelegramWebhook|TestAutomationSchemas'`; passed.
    - Ran `go test ./core -run 'TestAutomationTelegramMessageTriggerQueuesRuns|TestAutomationTelegramStepSendsMessage|TestAutomationSchemas|TestAutomationValidate'`; passed after adding `telegram` as a supported template root.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`; passed with existing Vite large chunk warning.
    - Reverted generated `ui/dist/index.html` asset hash churn from the verification build.
    - Ran `git diff --check`; passed.
    - Received follow-up request to add a button that registers the Telegram webhook and shows the response.
    - Added authenticated `POST /api/settings/telegram/register-webhook`.
    - Webhook registration uses saved Telegram credentials, calls Telegram `setWebhook`, limits updates to message-like events, and returns `ok`, `description`, `result`, status code, and a redacted webhook URL.
    - Added shared `telegramWebhookRegistration` UI component to both automation editor surfaces.
    - Added "Register webhook" button with loading state, success/rejection toast, and inline JSON response display.
    - Added helper and authenticated API tests for Telegram webhook registration.
    - Ran `gofmt` on touched settings API/test files; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/settings/automations/telegramWebhookRegistration.js src/settings/automations/pageAutomationUpsert.js src/settings/automations/automationUpsertModal.js`; passed.
    - Ran `node --check` for the registration component and both automation editor modules; passed.
    - Ran `go test ./apis -run 'TestRegisterTelegramWebhook|TestSettingsRegisterTelegramWebhook|TestSettingsTestCredentials|TestAutomationTelegramWebhook'`; passed.
    - Ran `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`; passed with existing Vite large chunk warning.
    - Reverted generated `ui/dist/index.html` asset hash churn from the verification build.
    - Ran `git diff --check`; passed.
  - Now:
    - Telegram webhook registration button/result display implementation and focused verification are complete.
  - Next:
    - Await user review or next requested Telegram automation enhancement.

Open questions (UNCONFIRMED if needed):

- None.

Working set (files/ids/commands):

- `CONTINUITY.md`
- `UI_DOCS.md`
- `core/settings_model.go`
- `core/settings_model_test.go`
- `apis/settings_test.go`
- `apis/settings.go`
- `apis/settings_credentials.go`
- `apis/settings_credentials_test.go`
- `ui/src/settings/credentials/pageCredentialsSettings.js`
- `ui/src/store.js`
- `ui/src/router.js`
- `core/automation_model.go`
- `core/automation_validate.go`
- `core/automation_schema.go`
- `core/automation_steps.go`
- `core/automation_telegram.go`
- `core/automation_templates.go`
- `core/automation_runner_test.go`
- `core/automation_model_test.go`
- `apis/automation.go`
- `apis/automation_test.go`
- `ui/src/base/automationInput.js`
- `ui/src/settings/automations/stepEditor.js`
- `ui/src/settings/automations/telegramStepForm.js`
- `ui/src/settings/automations/pageAutomationUpsert.js`
- `ui/src/settings/automations/automationUpsertModal.js`
- `ui/src/settings/automations/automationsList.js`
- `ui/src/settings/automations/automationRunsList.js`
- `ui/src/settings/automations/automationRunPreviewModal.js`
- `ui/src/settings/automations/telegramWebhookRegistration.js`
- `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/settings/credentials/pageCredentialsSettings.js src/store.js src/router.js`
- `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/settings/automations/stepEditor.js src/settings/automations/telegramStepForm.js`
- `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/base/automationInput.js src/settings/automations/pageAutomationUpsert.js src/settings/automations/automationUpsertModal.js src/settings/automations/automationsList.js src/settings/automations/automationRunsList.js src/settings/automations/automationRunPreviewModal.js`
- `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npx dprint fmt src/settings/automations/telegramWebhookRegistration.js src/settings/automations/pageAutomationUpsert.js src/settings/automations/automationUpsertModal.js`
- `node --check src/settings/credentials/pageCredentialsSettings.js`
- `node --check src/settings/automations/stepEditor.js`
- `node --check src/settings/automations/telegramStepForm.js`
- `env DPRINT_CACHE_DIR=/private/tmp/dprint-cache npm run build`
- `go test ./core ./apis -run 'TestSettings'`
- `go test ./apis -run 'TestSettingsTestCredentials|TestTestTelegramCredentials|TestTestGoogleSheetsCredentials'`
- `go test ./core -run 'TestAutomationTelegramStepSendsMessage|TestAutomationSchemas|TestAutomationValidate'`
- `go test ./core -run 'TestAutomationTelegramMessageTriggerQueuesRuns|TestAutomationTelegramStepSendsMessage|TestAutomationSchemas|TestAutomationValidate'`
- `go test ./apis -run 'TestAutomationTelegramWebhook|TestAutomationSchemas'`
- `go test ./apis -run 'TestRegisterTelegramWebhook|TestSettingsRegisterTelegramWebhook|TestSettingsTestCredentials|TestAutomationTelegramWebhook'`
- `go test ./...`
- `git diff --check`
